name: Legacy Release (retired)

on:
  workflow_dispatch:

permissions: {}

jobs:
  retired:
    name: retired
    runs-on: ubuntu-latest
    steps:
      - name: Reject legacy release entry point
        run: |
          echo "This workflow is retired. Run the Release Request workflow from main."
          exit 1
